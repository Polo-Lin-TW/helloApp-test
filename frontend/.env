# Frontend Environment Configuration
# Backend connection settings
VITE_BACKEND_HOST=helloapp_backend
VITE_BACKEND_PORT=8000

# Development override (will use localhost in dev mode)
# VITE_BACKEND_HOST=localhost
# VITE_BACKEND_PORT=8000